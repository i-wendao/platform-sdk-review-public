# Remaining Risks

- The phase is statically validated only; runtime SDK behavior is still unknown.
- No API/auth/sandbox/signing/database evidence exists for this adapter path.
- The new static evidence and verification-scope fields reduce ambiguity, but they do not prove official SDK capability.
- Any move from static modeling to runtime behavior still needs a separate higher-tier gate.

