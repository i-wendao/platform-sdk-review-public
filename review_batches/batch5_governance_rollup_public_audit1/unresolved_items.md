# Unresolved Items

## Residual risks

- Runtime SDK behavior remains unverified.
- API / auth / sandbox behavior remains unverified.
- The static apply result is not a runtime guarantee.
- Future adapter work may need a new Audit Gate if scope expands into runtime or credential handling.

## Open governance note

Model / Effort reporting is now user-confirmed. If not visible, record `unknown`; `unknown` is not a failure condition.

## Next rollup trigger

Trigger the next governance rollup on one of the following:

1. every 3 to 5 batches,
2. every phase closure,
3. every public export.

