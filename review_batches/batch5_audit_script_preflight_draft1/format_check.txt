# Format Check

## wc -l scripts/audit/preflight_v6.sh
```text
104 scripts/audit/preflight_v6.sh
```

## head -n 8 scripts/audit/preflight_v6.sh
```text
#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<'USAGE'
Usage: preflight_v6.sh [--expect-branch <branch>] [--expect-remote <name> <url>] [--require-clean]

Read-only git preflight for Tier 1 / Tier 2 batches.
```

## tail -n 8 scripts/audit/preflight_v6.sh
```text
    exit 1
  fi
fi

if [ "$REQUIRE_CLEAN" -eq 1 ] && [ -n "$STATUS" ]; then
  echo "ERROR: git working tree is not clean" >&2
  exit 1
fi
```

## grep structure summary
```text
4:usage() {
11:  --expect-branch <branch>      Require current branch to match <branch>.
12:  --expect-remote <name> <url>  Require git remote <name> fetch/push URL to match <url>.
13:  --require-clean              Require git status --short to be empty.
27:while [ "$#" -gt 0 ]; do
28:  case "$1" in
29:    --expect-branch)
38:    --expect-remote)
48:    --require-clean)
```
