# Runtime Not Verified Status

The following areas remain intentionally unverified:

- SDK client construction
- SDK import/runtime probe behavior
- Shopee API calls
- auth/signing flows
- sandbox flows
- `.env` / token / secret handling
- DB/raw/staging/DWD/mart writes
- runtime evidence generation

Current static evidence must not be interpreted as runtime verification.
