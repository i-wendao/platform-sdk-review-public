# Forbidden Until Approved

The following remain forbidden until a separate approved Tier 3+ batch exists:

- SDK client instantiation for live behavior validation
- Shopee API calls
- auth/signing execution
- sandbox execution
- `.env` reads
- token/secret reads
- DB/raw/staging/DWD/mart writes
- retry loops
- pagination scans
- broad endpoint discovery
- runtime evidence generation from live calls

External watch material must not drive runtime judgments unless the user and ChatGPT jointly approve that reference path.
